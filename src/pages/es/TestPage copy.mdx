---
layout: ../../layouts/BaseLayout.astro
title: Laboratorio de Migración TS
lang: es
---

import VectorCanvas from '../../components/VectorCanvas.astro'; // Import VectorCanvas for the protocol
import MathJax from '../../components/MathJax.astro'; // Import the MathJax component for rendering math
import TestPageScript from '../../components/TestPageScript.astro'; // Import the script loader component
import * as math from 'mathjs'; // Import the mathjs library for vector calculations

{/* Load the MathJax library in the browser */}
<MathJax />
{/* Execute the specific script for this page */}
<TestPageScript />

# Prueba de Arquitectura

## Versión Dinámica

export const a = [2, 4]; // Define vectors a and b
export const b = [1, -1];

Dados dos vectores $\vec a = (2, 4)$ y $\vec b = ($<input type="number" id="input-bx" value={b[0]} style="width: 40px; text-align: center;" />, <input type="number" id="input-by" value={b[1]} style="width: 40px; text-align: center;" />$)$, calcula su suma y representa el vector resultante $\vec v_{test}$.

$$
\vec v_{test} = \vec a + \vec b
$$

export const v_test = math.add(a, b); // Calculate the sum of vectors a and b
export const v_test_json = JSON.stringify(v_test); // Convert the result array to a JSON string to pass it as an HTML attribute

<figure class="centered">
    <svg id="svg_test_simple" data-vec={v_test_json}></svg>
    <figcaption>Prueba de vector [{v_test.join(', ')}]</figcaption>
</figure>

<br/><hr/><br/>

## Versión Estática (Original)

Dados dos vectores $\vec a = (${a.join(', ')}$)$ y $\vec b = (${b.join(', ')}$)$, calcula su suma y representa el vector resultante $\vec v_{test}$.

$$
\vec v_{test} = \vec a + \vec b
$$

<figure class="centered">
    <svg id="svg_test_static" data-vec={v_test_json}></svg>
    <figcaption>Versión estática: Vector [{v_test.join(', ')}]</figcaption>
</figure>
![Versión estática: Vector [3, 3]](vector:svg_test_static?data-vec=[3,3])
