---
layout: ../../layouts/BaseLayout.astro
title: TheHouseOfPhi Geometría
lang: es
---

import VectorCanvas from '../../components/VectorCanvas.astro'; // Contenedor para renderizar los gráficos SVG dinámicos
import MathJax from '../../components/MathJax.astro'; // Carga la librería MathJax para renderizar LaTeX dentro de los SVGs
import GeometryScript from '../../components/GeometryScript.astro'; // Injects specific TypeScript logic for the exercises on this page

<MathJax />
<GeometryScript />

# Anexo 1: Fundamentos Geométricos para la Física

[Inicio](/) > [Geometría](#1-introducción)

## 1. Introducción
La física y la geometría son disciplinas inseparables. Mientras la primera busca comprender las leyes naturales que rigen el universo, la segunda proporciona el lenguaje y el marco estructural necesario para cuantificar el espacio donde ocurren estos fenómenos.

### 1.1. Importancia de la geometría como base para la física
Desde el estudio de la caída de un grave hasta la descripción de las órbitas planetarias, todo movimiento sigue una trayectoria definida por propiedades geométricas. El dominio de la trigonometría es esencial para la descomposición de magnitudes vectoriales, mientras que el conocimiento de las cónicas es la clave para entender la gravitación universal. Este anexo actúa como una "caja de herramientas" técnica a la que el lector puede acudir para facilitar la transición entre la intuición física y la resolución matemática precisa.

### 1.2. Bloques fundamentales del anexo
Este recurso se organiza en los siguientes bloques de consulta:
- **[Sistema de coordenadas cartesianas](#2-sistema-de-coordenadas-cartesianas):** Representación de puntos y rectas en el plano.
- **[El Triángulo](#3-el-triángulo-razones-trigonométricas):** Propiedades básicas y teoremas fundamentales (Pitágoras, Seno y Coseno).
- **[El Círculo](#4-el-círculo):** Elementos del círculo y la definición del radián.
- **[Las Cónicas](#5-las-conicas):** (Parábola, elipse e hipérbola)Definición y aplicaciones en física.
- **A1.7 Sistema de coordenadas polares:** Localización mediante radio y ángulo.

## 2. Sistema de coordenadas cartesianas

La geometría analítica parte de la idea de que cualquier punto del plano puede describirse mediante dos números: su distancia horizontal y su distancia vertical respecto a dos ejes fijos (generalmente perpendiculares). Este sistema es la base de casi cualquier representación en Física, desde gráficas cinemáticas hasta mapas de campos.

Esto nos lleva al concepto de **sistema de coordenadas cartesianas** (o sistema de coordenadas rectangulares) en dos dimensiones es un marco de referencia que permite localizar cualquier punto en un plano de manera unívoca.

Este sistema está constituido por dos rectas numéricas perpendiculares entre sí, denominadas **ejes de coordenadas**, que se cortan en un punto llamado **origen (O)**.

- **Eje de abscisas (Eje x):** Es la recta horizontal. Los valores positivos se encuentran a la derecha del origen.
- **Eje de ordenadas (Eje y):** Es la recta vertical. Los valores positivos se encuentran por encima del origen.

Cualquier punto $P$ en el plano se describe mediante un par ordenado de números $(x, y)$, llamados **coordenadas**:

$$
P = (x, y) \tag{2.1}
$$

Donde $x$ representa la distancia perpendicular al eje $y$, y $y$ representa la distancia perpendicular al eje $x$. Estos dos ejes dividen al plano en cuatro regiones llamadas **cuadrantes**, numerados del I al IV en sentido antihorario partiendo del cuadrante superior derecho.

La verdadera potencia de este sistema reside en que establece un puente directo entre la geometría y el álgebra. Gracias a las coordenadas, podemos describir figuras geométricas (como rectas, círculos o parábolas) mediante ecuaciones algebraicas, y viceversa. Este enfoque, conocido como **geometría analítica**, nos permite resolver problemas geométricos utilizando las herramientas del álgebra, como el cálculo de distancias, la determinación de puntos de intersección o el análisis de las propiedades de las curvas.

Este sistema nos permite traducir posiciones físicas reales —por ejemplo, en una mesa de laboratorio o a lo largo de una regla— a una representación matemática precisa.

La representación en coordenadas \(x, y\) también se puede expresar como un **vector de posición** $\vec r = (x, y)$ respecto a un origen fijo. Esto permite expresar distancias, desplazamientos y cambios de forma compacta.

{/* --- Initial values for A1_1 example --- */}
export const exaA1_1_P = [2, 4];

Veamos un ejemplo. La siguiente figura muestra un sistema de coordenadas cartesianas, con el eje de abcisas (Eje x), el eje de ordenadas (Eje y) y el origen (O). Además, se marcan los cuadrantes del plano. En este sitema de coordenadas, mostramos un punto P, de coordenadas (<input type="number" id="input-exaA1_1_Px" value={exaA1_1_P[0]} min="-10" max="10" style="width: 40px"/>, <input type="number" id="input-exaA1_1_Py" value={exaA1_1_P[1]} min="-10" max="10" style="width: 40px"/>)

{/* --- Converts to string for passing to the drawing function--- */}
export const exaA1_1_P_json = JSON.stringify(exaA1_1_P);

<figure class="centered">
    <VectorCanvas id="svgA1_1"
        data-point-coordinates={exaA1_1_P_json}
    />
    <figcaption><b>Figura A1.1</b>: Representación de un punto, sus coordenadas y su vector posición en el plano.</figcaption>
</figure>

{/* ---![Figura 2.1: Plano cartesiano con ejes X e Y, cuadrantes, un punto genérico P, de coordenadas x e y, y su vector de posición r.](vector:svgA1_1) --- */}


### Punto medio
El **punto medio** de dos posiciones permite describir centros geométricos o puntos de equilibrio y se calcula mediante:
$$
M = \left(\frac{x_1 + x_2}{2}, \frac{y_1 + y_2}{2}\right). \tag{2.2}
$$
O, mediante los vectores de posición:
$$
\vec r_M = \tfrac{1}{2}(\vec r_1+\vec r_2)\ \tag{2.3}
$$

### 2.1 Ecuaciones de la recta
Cada forma de la recta es útil para un tipo de problema distinto.

#### **Forma explícita:**
$$
y = mx + n \tag{2.4}
$$
- **m** es la pendiente: indica cuánto crece y por cada unidad que avanza x.
- **n** es la ordenada en el origen: valor de y cuando x = 0.

#### **Forma punto–pendiente:** 
$$
y - y_0 = m(x - x_0)  \tag{2.5}
$$
Muy útil cuando conocemos un punto concreto de la recta.

#### **Forma general:**
$$
Ax + By + C = 0  \tag{2.6}
$$
La más adecuada cuando queremos:
- identificar paralelismo o perpendicularidad,
- usar métodos algebraicos,
- calcular distancias.
El vector **normal** a la recta es \(\vec n=(A,B)\).

#### **Forma Paramétrica/Vectorial:**

La ecuación paramétrica es una de las formas más intuitivas y potentes de describir una recta en el plano. En lugar de expresar la relación entre `x` e `y` mediante una única ecuación, la recta se representa como el conjunto de todos los puntos que se obtienen al desplazar un punto fijo en una dirección determinada.

Para definir una recta en forma paramétrica necesitamos dos elementos esenciales:

- **Un punto de la recta**, llamado *punto base* o *punto de apoyo*:

  $$A(a_1, a_2)$$

- **Un vector director**, que indica la dirección de la recta:

  $$\vec v = (v_1, v_2)$$

Este vector director es fundamental: determina hacia dónde avanza la recta y cómo se generan todos sus puntos.

Para construir la ecuación paramétrica de una recta, partimos del punto `A` y nos desplazamos en la dirección del vector `v`, tantas veces como indique un parámetro real `λ`. Entonces, la ecuación paramétrica queda así:

$$
\begin{cases}
x = a_1 + \lambda v_1 \\
y = a_2 + \lambda v_2
\end{cases}
\quad \lambda \in \mathbb{R}
$$

Cada valor de `λ` genera un punto distinto de la recta.

La ecuación paramétrica muestra explícitamente cómo se recorre la recta. Es especialmente útil en:
- geometría analítica
- física
- programación gráfica
- álgebra lineal

{/* --- Valores del punto base, el vector director y t (pueden ser modificados por el lector) --- */}
export const svgA1_2_t = 0;
export const svgA1_2_A = [-4, -2];
export const svgA1_2_v = [3, 1];

Veamos un ejemplo: Supongamos que tenemos un punto base A(<input type="number" id="input-svgA1_2_Ax" value={svgA1_2_A[0]} min="-15" max="15" style="width: 40px"/>, <input type="number" id="input-svgA1_2_Ay" value={svgA1_2_A[1]} min="-15" max="15" style="width: 40px"/>) y un vector director $\vec v$ = (<input type="number" id="input-svgA1_2_vx" value={svgA1_2_v[0]} min="-10" max="10" style="width: 40px; text-align: center;"/>, <input type="number" id="input-svgA1_2_vy" value={svgA1_2_v[1]} min="-10" max="10" style="width: 40px; text-align: center;"/>), la recta queda:

$ x = $<span id="output-svgA1_2_Ax">{svgA1_2_A[0]}</span>$ + $<span id="output-svgA1_2_vx">{svgA1_2_v[0]}</span>$\lambda$

$ y = $<span id="output-svgA1_2_Ay">{svgA1_2_A[1]}</span>$ + $<span id="output-svgA1_2_vy">{svgA1_2_v[1]}</span>$\lambda$



$$
\begin{cases}
x = -4 + 3\lambda \\
y = -2 + \lambda
\end{cases}
$$


Se escribe como:
$$
x = x_0 + ta, \quad y = y_0 + tb. \tag{2.8}
$$
Aquí, \(t\) es un parámetro que describe el avance a lo largo de la recta.
O como:  
$$
\vec r(t)=\vec r_0 + t\,\vec v,\quad t\in\mathbb{R}, \tag{2.9}
$$
siendo $\vec r_0$ un punto de la recta y $\vec v$ un vector **dirección** (no nulo).
  
Relaciones útiles: si $\vec v=(a,b)$, entonces la pendiente $m=b/a$ (si $a\neq0$).



//export const e1_vRiver_json = JSON.stringify(svgA1_2_t);


**Figura 2.2**: *Recta dada por \(\vec r(t)=\vec r_0+t\vec v\), mostrando \(\vec v\) como incremento por unidad de \(t\).* 

## 3. El triángulo, Razones trigonométricas
El triángulo es el polígono más simple y la unidad fundamental para la descomposición de casi cualquier magnitud física vectorial en el plano.

### 3.1. Elementos básicos del triángulo y nomenclatura
Un triángulo es un polígono de tres lados que encierra una región del plano. Sus elementos fundamentales son los vértices, los lados y los ángulos. Para facilitar la resolución de problemas, se utiliza una **nomenclatura clásica** que relaciona estos elementos entre sí:

 - **Vértices:** Son los puntos de intersección de los lados. Se representan con letras mayúsculas: $A$, $B$ y $C$.
 - **Lados:** Son los segmentos que unen los vértices. Se representan con letras minúsculas: $a$, $b$ y $c$. La convención dicta que **el lado $a$ es el opuesto al vértice $A$**, el lado $b$ es el opuesto al vértice $B$, y así sucesivamente.
 - **Ángulos:** Son las aperturas entre dos lados contiguos. Se pueden nombrar usando letras griegas ($\alpha$, $\beta$, $\gamma$) o mediante el nombre del vértice con un acento circunflejo ($\hat A$, $\hat B$, $\hat C$). Al igual que los lados, el ángulo $\alpha$ (o $\hat A $) se localiza en el vértice $A$.

Una propiedad ineludible en geometría euclidiana es que la suma de los ángulos internos de cualquier triángulo siempre es constante:

$$
\alpha + \beta + \gamma = 180^\circ = \pi \text{ rad} \tag{3.1}
$$

![Figura 3.1: Triángulo ABC con sus vértices, lados y ángulos.](vector:svgA1_3_1)

#### **Semejanza de triángulos**
Dos triángulos son semejantes si tienen ángulos iguales y lados proporcionales. Esta propiedad es vital en física para resolver problemas de óptica, sombras y diagramas de fuerzas mediante razones de proporcionalidad.

### 3.2 Razones trigonométricas en el triángulo rectángulo
En un triángulo rectángulo, las relaciones entre los ángulos y las longitudes de sus lados no son arbitrarias; se rigen por proporciones constantes denominadas **razones trigonométricas**. Estas razones permiten determinar distancias inaccesibles y son la base de la descomposición de vectores.

#### **Definición de seno, coseno y tangente**
Considerando un triángulo rectángulo y uno cualquiera de sus dos ángulos agudos, al que llamamos $\theta$, nombramos los lados de este triángulo respecto a dicho ángulo como:

 - **Hipotenusa ($h$):** El lado más largo, opuesto al ángulo recto.
 - **Cateto opuesto ($c_o$):** El lado que se encuentra frente al ángulo $\theta$.
 - **Cateto contiguo o adyacente ($c_a$):** El lado que forma parte del ángulo $\theta$ (junto a la hipotenusa).

![Figura 3.1: Posición del cateto contiguo o adyacente y del cateto opuesto, respecto del ángulo θ.](vector:svgA1_2_1)

#### ⚠️ **Importante: Los catetos dependen del ángulo elegido**
Es crucial entender que los términos "opuesto" y "contiguo" **no son fijos**; dependen enteramente de qué ángulo agudo estemos utilizando como referencia.

Si en lugar del ángulo $\theta$ (en la base) usáramos el otro ángulo agudo (el superior), los papeles se invertirían: el lado que antes era "opuesto" pasaría a ser el "contiguo" a ese nuevo ángulo, y viceversa. La hipotenusa, sin embargo, nunca cambia.

**Regla de oro para física:** Nunca memorices que el seno es "vertical" o el coseno "horizontal". Siempre pregúntate: "¿Este cateto está enfrente de mi ángulo (seno) o lo está tocando (coseno)?".

Las razones principales (respecto al ángulo elegido, $\theta$) se definen como:

$$
\sin \theta = \frac{\text{cateto opuesto}}{\text{hipotenusa}} = \frac{c_o}{h} \tag{3.2}
$$

$$
\cos \theta = \frac{\text{cateto contiguo}}{\text{hipotenusa}} = \frac{c_a}{h} \tag{3.3}
$$

$$
\tan \theta = \frac{\text{cateto opuesto}}{\text{cateto contiguo}} = \frac{c_o}{c_a} \tag{3.4}
$$

Podemos imaginar las razones trigonométricas como "factores de escala": el **coseno** nos indica qué fracción de la hipotenusa se proyecta sobre el eje horizontal, mientras que el **seno** indica qué fracción se proyecta sobre el eje vertical. La **tangente**, por su parte, define la pendiente o inclinación de la hipotenusa respecto a la base.

#### **Identidades fundamentales**
Existen relaciones matemáticas que siempre se cumplen, independientemente del tamaño del triángulo:

- **Identidad pitagórica:** Derivada directamente del Teorema de Pitágoras, es la identidad más importante de la trigonometría:
$$
\sin^2 \theta + \cos^2 \theta = 1 \tag{3.5}
$$

- **Relación entre razones:** La tangente puede expresarse siempre como el cociente entre el seno y el coseno:
$$
\tan \theta = \frac{\sin \theta}{\cos \theta} \tag{3.6}
$$

- **Ángulos complementarios:** Dado que la suma de los ángulos agudos en un triángulo rectángulo es 90º, se cumple que el seno de un ángulo es igual al coseno de su complementario:
$$
\sin \theta = \cos(90^\circ - \theta) \tag{3.7}
$$

#### **Aplicaciones**
En física, la trigonometría no es solo una herramienta de cálculo, sino un método para entender cómo se distribuyen las magnitudes en el espacio.
- **Cálculo de alturas y distancias:** Permite hallar la altura de un proyectil o la distancia a un objeto conociendo solo el ángulo de observación y una base medida.
- **Planos inclinados:** Es fundamental para descomponer la fuerza del peso ($P = mg$) en una componente paralela al plano ($mg \sin \theta$) que genera movimiento, y una componente normal ($mg \cos \theta$) que genera rozamiento.
- **Descomposición de fuerzas:** Cualquier fuerza ejercida con un ángulo inclinado (como la tensión de una cuerda) se divide en una componente horizontal y una vertical mediante el uso de seno y coseno.

### 3.3. Teorema de Pitágoras
Establece que en un triángulo rectángulo, el cuadrado de la hipotenusa ($c$) es igual a la suma de los cuadrados de los catetos ($a$ y $b$):

El **teorema de Pitágoras** establece que en cualquier triángulo rectángulo $\triangle ABC$, donde $c$ es la longitud de la hipotenusa y $a$ y $b$ son las longitudes de los catetos,

$$
c^2 = a^2 + b^2 \tag{3.8}
$$

![Figura 3.2: Triángulo rectángulo con catetos a y b, e hipotenusa c.](/images/RightTriangle.svg)

#### **Distancia entre dos puntos**
Aplicando el teorema de Pitágoras, calculamos la distancia entre dos puntos del plano:
$$
AB = \sqrt{(x_2 - x_1)^2 + (y_2 - y_1)^2} \tag{3.9}
$$
Este cálculo aparece continuamente en Física: distancias entre posiciones, magnitudes de desplazamientos, longitudes de trayectorias idealizadas, etc.

### 3.4. Teorema del seno y teorema del coseno
Cuando el triángulo no es rectángulo, utilizamos relaciones más generales para determinar lados o ángulos desconocidos.

**Teorema del Coseno:** Se aplica cuando conocemos dos lados y el ángulo que forman ($C$). Es la base de la ley del paralelogramo en la suma de vectores.
$$
c^2 = a^2 + b^2 - 2ab \cdot \cos(C) \tag{3.10}
$$

**Teorema del Seno:** Establece la proporción entre los lados y los senos de sus ángulos opuestos:
$$
\frac{a}{\sin(A)} = \frac{b}{\sin(B)} = \frac{c}{\sin(C)} \tag{3.11}
$$

## 4. El círculo
El círculo es una de las figuras más presentes en la física, desde el estudio de las órbitas y motores hasta la propagación de ondas. Mientras que la **circunferencia** es la línea curva cerrada donde todos los puntos están a la misma distancia del centro, el **círculo** es la superficie contenida dentro de ella.

**Definición de circunferencia**  
El conjunto de puntos del plano que están a una distancia fija \(R > 0\) de un punto fijo \(\vec c = (h,k)\), llamado **centro**:
$$
\lVert \vec r - \vec c \rVert = R, \quad \text{con } \vec r=(x,y). \tag{4.1}
$$

### 4.1. Elementos del círculo
Para describir un círculo y sus partes, utilizamos los siguientes conceptos:
- **Radio ($r$):** Segmento que une el centro con cualquier punto de la circunferencia.
- **Diámetro ($d$):** Segmento que une dos puntos de la circunferencia pasando por el centro. Su longitud es el doble del radio ($d = 2r$).
- **Cuerda:** Segmento que une dos puntos de la circunferencia sin necesidad de pasar por el centro.
- **Arco ($s$):** Porción de la circunferencia comprendida entre dos puntos.
- **Sector circular:** "Porción de tarta" del círculo delimitada por dos radios y un arco.

![Figura 4.1: Elementos del círculo.](vector:svgA1_3_1)

#### **Longitud de la circunferencia y área**
La relación entre la longitud de una circunferencia ($L$) y su diámetro es una constante universal llamada **$\pi$ (pi)**. De forma intuitiva, $\pi$ (aprox. 3.14159...) representa cuántas veces cabe el diámetro en el perímetro de su círculo.

$$
L = 2\pi r \tag{4.2}
$$

$$
\text{Área} = \pi r^2 \tag{4.3}
$$

### 4.2. Ángulos en la circunferencia
En el análisis de movimientos rotatorios, es fundamental distinguir cómo medimos los ángulos:
- **Ángulo central:** Es aquel cuyo vértice está en el centro de la circunferencia y sus lados son dos radios. La medida del arco interceptado es directamente proporcional a este ángulo.
- **Ángulo inscrito:** Es aquel cuyo vértice está en un punto de la circunferencia y sus lados son dos cuerdas.

**Relación fundamental:** Para un mismo arco, el ángulo central mide exactamente el doble que el ángulo inscrito.

### 4.3. Radianes
Aunque en la vida cotidiana usamos los grados sexagesimales (0° a 360°), en física la unidad natural de medida es el **radián (rad)**.

#### **Definición**
Un radián es el ángulo central subtendido por un arco cuya longitud ($s$) es igual al radio ($r$) de la circunferencia.

#### **Conversión grados ↔ radianes**
Puesto que una vuelta completa (360°) equivale a la longitud total de la circunferencia ($2\pi r$), la equivalencia fundamental es:

$$
360^\circ = 2\pi \text{ rad} \implies 180^\circ = \pi \text{ rad} \tag{4.4}
$$

#### **¿Por qué en física se usan radianes?**
El uso del radián no es caprichoso. Su principal ventaja es que simplifica las ecuaciones de movimiento, ya que permite relacionar directamente magnitudes lineales y angulares sin factores de conversión extraños. Por ejemplo, la longitud de un arco se calcula simplemente como:

$$
s = \theta \cdot r \tag{4.5}
$$

Esta relación solo es válida si el ángulo $\theta$ está expresado en **radianes**.

### 4.4. Ecuaciones de la circunferencia

### **Ecuación canónica (cartesiana)**
$$
(x - h)^2 + (y - k)^2 = R^2. \tag{4.6}
$$

### **Ecuación general (cartesiana)**
$$
x^2 + y^2 + D x + E y + F = 0, \tag{4.7}
$$
que se lleva a la forma canónica completando cuadrados:
$$
(x - h)^2 + (y - k)^2 = R^2,\qquad h = -\tfrac{D}{2}, \quad k = -\tfrac{E}{2}, \quad R^2 = h^2 + k^2 - F \tag{4.8}
$$

### **Forma vectorial**
$$
\lVert \vec r - \vec c \rVert^2 = R^2,\qquad \vec r=(x,y),\; \vec c=(h,k) \tag{4.9}
$$

### **Ecuación paramétrica de la circunferencia**
La forma paramétrica describe la circunferencia recorriéndola mediante un ángulo $\theta$:
$$
\begin{cases}
x(\theta) = h + R \cos\theta, \\
y(\theta) = k + R \sin\theta,
\end{cases}
\qquad \theta \in [0, 2\pi).
$$

**Propiedades:**
- Para $\theta = 0$, el punto corresponde a $(h+R, k)$.
- El recorrido se hace en **sentido antihorario** al aumentar \(\theta\).
- Puede añadirse un desfase angular:
$$
x(\theta)=h+R\cos(\theta+\theta_0),\qquad y(\theta)=k+R\sin(\theta+\theta_0).
$$
- Si se interpreta $\theta$ como parámetro temporal:
$$
\frac{d\vec r}{d\theta} = (-R\sin\theta, R\cos\theta),\qquad 
\left\lVert\frac{d\vec r}{d\theta}\right\rVert = R,
$$
es decir, la rapidez respecto al parámetro $\theta$ es constante.

## 5. Las Cónicas

Las **cónicas** constituyen una familia de curvas fundamentales tanto en Matemáticas como en Física. Aunque se originan como secciones producidas al cortar un cono con un plano, su relevancia trasciende esta interpretación geométrica: aparecen en la **óptica geométrica**, en la **mecánica celeste**, en el **estudio de campos**, en el diseño de **sistemas ópticos**, en trayectorias de partículas y en múltiples contextos más. 

En la sección anterior se estudió el caso particular del **círculo**, que es una cónica de excentricidad nula. Ahora ampliaremos esta visión abordando las otras tres cónicas importantes: **parábola**, **elipse** e **hipérbola**. Aunque sus ecuaciones parezcan diferentes, todas comparten una estructura geométrica común basada en relaciones de distancia respecto a focos, directrices y simetrías.


### 5.1. La Parábola

La **parábola** es la cónica más directamente vinculada a fenómenos de reflexión y concentración de energía. Su forma está determinada por una propiedad métrica muy sencilla: cada punto de la parábola es **equidistante** del **foco** $\vec f$ y de una **directriz**, que es una recta definida por $\vec n \cdot \vec r + d = 0$.

Esta condición métrica puede escribirse como:
$$
\lVert \vec r - \vec f \rVert = \frac{|\vec n \cdot \vec r + d|}{\lVert \vec n \rVert}.
$$

Cuando elegimos un sistema de ejes adecuado (foco en $(p,0)$, directriz en $x=-p$), esta definición adopta la conocida forma **canónica**:
$$
y^2 = 4px.
$$

La parábola aparece en numerosos contextos físicos:
- **Óptica geométrica**: un haz de rayos paralelos converge en el foco tras reflejarse.
- **Antenas parabólicas y radioastronomía**: concentración de ondas electromagnéticas en el receptor.
- **Reflectores de telescopios**: focalización precisa de luz estelar.
- **Movimiento parabólico**: trayectorias balísticas en campos gravitatorios uniformes (aproximación de primer orden).

**Figura 8**: *Parábola con foco, directriz (mostrando $\vec n$) y eje de simetría.*


### 5.2. La Elipse

La **elipse** es quizá la cónica más relevante para la física gravitatoria. Se define por la condición de que la **suma de las distancias** a dos focos $\vec f_1,\vec f_2$ es constante:
$$
\lVert \vec r - \vec f_1 \rVert + \lVert \vec r - \vec f_2 \rVert = 2a.
$$

Este comportamiento métrico se traduce, al elegir ejes principales, en la ecuación canónica:
$$
\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1,
$$
con focos situados en $(\pm c,0)$ y relación entre parámetros dada por $c^2 = a^2 - b^2$.

Su presencia en Física es extensa:
- **Órbitas celestes**: según la *primera ley de Kepler*, los planetas describen elipses con el Sol en uno de sus focos.
- **Sistemas ópticos y acústicos**: en una sala elíptica, un sonido emitido desde un foco se concentra en el otro.
- **Conservación de energía y momento angular** en mecánica lagrangiana aplicada a gravedad newtoniana.

**Figura 9**: *Elipse con focos, semiejes y centros; construcción métrica por suma de distancias.*


### 5.3. La Hipérbola

La **hipérbola** complementa la familia de cónicas asociándose a situaciones donde existe un exceso de energía que permite trayectorias de escape. Su definición métrica establece que la **diferencia de distancias** a dos focos es constante:
$$
\big|\lVert\vec r - \vec f_1\rVert - \lVert\vec r - \vec f_2\rVert \big| = 2a.
$$

Con los ejes alineados, su ecuación canónica es:
$$
\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1,
$$
y sus asíntotas vienen dadas por las rectas $y = \pm (b/a)\, x$.

Desde la perspectiva física:
- Describe **órbitas de escape** de cuerpos con energía mecánica positiva.
- Las **asíntotas** modelan direcciones límite en procesos de dispersión gravitatoria.
- Modela curvas equipotenciales en sistemas donde existe simetría hiperbólica.

**Figura 10**: *Hipérbola con vértices, focos y asíntotas.*


### 5.4. Relación entre Cónicas y Excentricidad

Aunque las parábolas, elipses e hipérbolas parezcan objetos muy distintos, todas ellas pueden interpretarse como **secciones de un mismo cono geométrico**. La clave que las unifica es su **excentricidad** $e$, un parámetro que mide cuánto "se aleja" la cónica del caso circular:
- $0 < e < 1$: **elipse**.
- $e = 1$: **parábola**.
- $e > 1$: **hipérbola**.

Más allá de su origen geométrico, todas pueden transformarse entre sí mediante **cambios de coordenadas** adecuados. Si una cónica está dada en un sistema arbitrario, es posible llevarla a su forma canónica mediante rotación y traslación:
$$
\vec r = R\,\vec r' + \vec r_c,
$$
donde $R$ es una matriz de rotación y $\vec r_c$ un vector de traslación. Este procedimiento revela la estructura fundamental de la cónica independientemente de su orientación o posición.

**Figura 11**: *Cono y planos de corte que generan cada tipo de cónica.*

---


### X. Perpendicularidad entre un vector con módulo constante y su variación temporal
#### Introducción:
En diversas áreas de la física, la mecánica y la cinemática, nos encontramos con frecuencia con el análisis de **vectores cuyo módulo permanece constante en el tiempo**, aunque su dirección pueda variar. Esto incluye vectores de posición en movimientos restringidos a una superficie esférica o circular, así como vectores unitarios que cambian de orientación, como el vector unitario tangencial en el estudio de trayectorias curvas. En estos escenarios, surge una propiedad geométrica fundamental: **la variación temporal de cualquier vector de módulo constante es siempre perpendicular al vector original**. Esta propiedad, con raíces en la geometría, tiene amplias y significativas aplicaciones en física, facilitando la comprensión de conceptos como la velocidad y la aceleración en movimientos curvilíneos, la descomposición de la aceleración en componentes intrínsecas, y en general, el análisis de sistemas físicos donde ciertas magnitudes vectoriales se conservan constantes. En este artículo, exploraremos y demostraremos esta propiedad esencial, utilizando dos perspectivas complementarias: una demostración geométrica intuitiva basada en el triángulo isósceles y una demostración analítica rigurosa empleando la función paramétrica.

#### Enfoque Geométrico Intuitivo - El Triángulo Isósceles
Consideremos un vector $\vec V(t)$ genérico, que varía en el tiempo *t*, pero cuyo módulo se mantiene constante, es decir, $|\vec V(t)| = C$, donde C es una constante escalar. Visualicemos el vector $\vec V(t)$ y un vector $\vec V(t + \Delta t)$ en un instante de tiempo ligeramente posterior, *t + Δt*. Debido a la constancia del módulo, las puntas de estos vectores se moverán restringidas a una superficie esférica (o circular en 2D) de radio C, centrada en el origen.

[Image of Unit Vectors Changing Direction]

El cambio en el vector $\vec V(t)$ durante este pequeño intervalo de tiempo Δt es el vector diferencia $\Delta \vec V = \vec V(t + \Delta t) - \vec V(t)$. Geométricamente, $\Delta \vec V$ representa el vector que une la punta de $\vec V(t)$ con la punta de $\vec V(t + \Delta t)$.

[Image of Isosceles Triangle Unit Vectors]

Si consideramos el triángulo vectorial formado por $\vec V(t)$, $\vec V(t + \Delta t)$ y $\Delta \vec V$ (más precisamente, los vectores $\vec V(t)$, $-\vec V(t)$ y $\vec V(t + \Delta t)$), notamos que es un triángulo isósceles, ya que $|\vec V(t)| = |\vec V(t + \Delta t)| = C$. El vector $\Delta \vec V$ es la base de este triángulo isósceles, con $\vec V(t)$ y $\vec V(t + \Delta t)$ como los lados iguales.

Consideremos el ángulo Δθ entre $\vec V(t)$ y $\vec V(t + \Delta t)$. En el triángulo isósceles, los ángulos opuestos a los lados iguales (adyacentes a $\Delta \vec V$) son iguales, llamémoslos α. La suma de los ángulos internos de un triángulo es $\pi$ radianes:

$$
2\alpha + \Delta\theta = \pi \eqtag{5.5}
$$

De donde obtenemos:

$$
\alpha = \frac{(\pi - \Delta\theta)}{2} = \frac{\pi}{2} - \frac{\Delta\theta}{2} \eqtag{5.6}
$$

Ahora, analicemos el límite cuando el intervalo de tiempo Δt tiende a cero (Δt → 0, y por tanto Δθ → 0). En este límite, $\Delta \vec V$ se convierte en el diferencial $d\vec V$, y Δθ se convierte en dθ. El ángulo α se aproxima a:

$$
\alpha \rightarrow \frac{\pi}{2} - 0 = \frac{\pi}{2} \text{ radianes o } 90^\circ. \eqtag{5.7}
$$

Esto implica que, en el límite infinitesimal, el ángulo entre el vector de cambio $d\vec V$ y el vector original $\vec V(t)$ tiende a 90 grados. Es decir, la variación infinitesimal $d\vec V$ es perpendicular al vector $\vec V(t)$. Como la derivada $d\vec V/dt$ (que representa la tasa de variación temporal) tiene la misma dirección que $d\vec V$, concluimos geométricamente que la tasa de variación temporal de $\vec V(t)$ es perpendicular a $\vec V(t)$ en cada instante, siempre que el módulo de $\vec V(t)$ se mantenga constante.

#### Enfoque Analítico - Función Paramétrica Generalizada con Parámetro Temporal
Para una demostración analítica más general, consideremos un vector $\vec V(t)$ en tres dimensiones (la demostración es igualmente válida en dos dimensiones), expresado en componentes cartesianas:

$$
\vec V(t) = V_x(t) \mathbf{i} + V_y(t) \mathbf{j} + V_z(t) \mathbf{k} \tag{5.8}
$$

La condición de módulo constante $|\vec V(t)| = C$ = constante, se expresa como:

$$
|\vec V(t)|^2 = V_x(t)^2 + V_y(t)^2 + V_z(t)^2 = C^2 \tag{5.9}
$$

Para que esta condición se mantenga en el tiempo, la derivada temporal de $|\vec V(t)|^2$ debe ser cero. Derivamos ambos lados de la ecuación con respecto al tiempo, utilizando la regla de la cadena:

$$
\frac{d}{dt} (|\vec V(t)|^2) = \frac{d}{dt} (C^2) \tag{5.10}
$$
$$
\frac{d}{dt} (V_x(t)^2 + V_y(t)^2 + V_z(t)^2) = 0 \tag{5.11}
$$

Aplicando la regla de la cadena a cada término:

$$
2V_x(t) \frac{dV_x(t)}{dt} + 2V_y(t) \frac{dV_y(t)}{dt} + 2V_z(t) \frac{dV_z(t)}{dt} = 0 \tag{5.12}
$$

Dividiendo por 2 y reordenando:

$$
V_x(t) \frac{dV_x(t)}{dt} + V_y(t) \frac{dV_y(t)}{dt} + V_z(t) \frac{dV_z(t)}{dt} = 0 \tag{5.13}
$$

El lado izquierdo de esta ecuación es precisamente el producto escalar del vector $\vec V(t)$ con su derivada temporal $\displaystyle\frac{d\vec V}{dt}$

$$
\vec V(t) \cdot \frac{d\vec V(t)}{dt} = \left( V_x(t), V_y(t), V_z(t) \right) \cdot \left( \frac{dV_x(t)}{dt}, \frac{dV_y(t)}{dt}, \frac{dV_z(t)}{dt} \right) = V_x(t) \frac{dV_x(t)}{dt} + V_y(t) \frac{dV_y(t)}{dt} + V_z(t) \frac{dV_z(t)}{dt} \tag{5.14}
$$

Por lo tanto, la condición de módulo constante $|\vec V(t)| = C$ implica que:

$$
\vec V(t) \cdot \frac{d\vec V(t)}{dt} = 0 \tag{5.15}
$$

Esto demuestra analíticamente que **el vector $\vec V(t)$ y su derivada temporal $\displaystyle\frac{d\vec V(t)}{dt}$ son siempre perpendiculares entre sí**, bajo la condición de que el módulo de $\vec V(t)$ se mantenga constante. Este resultado es válido para cualquier vector con módulo constante, no solo para vectores de posición.

#### Conclusión
Hemos demostrado, a través de dos enfoques complementarios, que **la variación temporal de un vector de módulo constante es intrínsecamente perpendicular al vector original**. Esta propiedad fundamental, visualizada geométricamente y confirmada analíticamente, trasciende el caso particular del vector posición y se aplica a cualquier vector cuyo módulo se conserve constante durante su evolución temporal.

Esta relación de perpendicularidad es de gran relevancia en física. Permite comprender mejor la naturaleza de la velocidad en movimientos curvilíneos, la dirección de la aceleración normal en términos de cambio de dirección de la velocidad, y proporciona una base sólida para el análisis de sistemas donde la conservación de magnitudes vectoriales, como la longitud de un vector, juega un papel fundamental. La propiedad aquí demostrada no es solo un resultado geométrico, sino una herramienta conceptual poderosa para el análisis y la interpretación de fenómenos físicos diversos en mecánica, electromagnetismo, y otras áreas de la física.

## Referencias
- [The Pitagorean Theorem. Prealgebra 2e. 9.3. Openstax.](https://openstax.org/books/prealgebra-2e/pages/9-3-use-properties-of-angles-triangles-and-the-pythagorean-theorem#:~:text=of%20the%20hypotenuse.-,THE%20PYTHAGOREAN%20THEOREM,-In%20any%20right)
- [Pythagorean theorem. Encyclopedia of Mathematics.](http://encyclopediaofmath.org/index.php?title=Pythagorean_theorem&oldid=40035)
- [Sine. Encyclopedia of Mathematics.](https://encyclopediaofmath.org/wiki/Sine#:~:text=the%20hyperbolic%20sine.-,Comments,-Of%20course%2C)
- [Sine, Cosine and Tangent. Mathisfun.](https://www.mathsisfun.com/algebra/trigonometry.html#:~:text=gives%20the%20answers!-,Sine%2C%20Cosine%20and%20Tangent,-The%20main%20functions)
- [Using Right Triangles to Evaluate Trigonometric Functions. Openstax.](https://openstax.org/books/algebra-and-trigonometry/pages/7-2-right-triangle-trigonometry#:~:text=Using%20Right%20Triangles%20to%20Evaluate%20Trigonometric%20Functions)
